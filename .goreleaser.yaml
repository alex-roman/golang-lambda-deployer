project_name: golang-lambda-deployer 

builds:
  - id: golang-lambda-deployer
    main: ./cmd/main.go
    goos:
      - linux
      - windows
      - darwin
    goarch:
      - amd64
      - arm64
    ldflags:
      - -s -w

archives:
  - format: tar.gz
    name_template: "{{ .ProjectName }}_{{ .Version }}_{{ .Os }}_{{ .Arch }}"
    files:
      - LICENSE
      - README.md

release:
  github:
    owner: alex-roman
    name: golang-lambda-deployer

changelog:
  sort: asc
  filters:
    exclude:
      - '^docs:'
      - '^test:'
